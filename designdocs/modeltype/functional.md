The functional model only describes the what and how not the where. Similar to user stories. 
Open: Hoe to test inside this model?

It uses Algebraic Data Types to describe the data.
And functions to the describe what is happening and when

f: (X) -> Y
   Documentation:
   Prompt:

open: work with relations
instead of functions more like method calls

properties of types
stereotypes

We need probably the concept of variables e.g
val x = f(y) ?
to say what shoukld happen with the result?

Variations
as higher order functions:

variants:
    variation() ->
Similar to switch case

State, External System algorithm, collections as algebraic data types:

repo.save()
repo.commit()

or as type with annotation
f: repo,state -> Unit
state(repo)

better repo.save()
clearly modeled outside
state machines are modeled outside this model


additional outside serializable description to map the functionality to classes







