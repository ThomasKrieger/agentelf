variations at functions/activities:
at types we have either algebaric or a implements b
We need to specify what is the sane and what changes
so two types for activites:
eith extensionpoints (similar to abstract methods)
with variations (or + and for stuff whicxh is the same for all)
similar to algebaric data types


as the order can be simply given by the order the statements appear 
in the text the best way is to hate to types:
extensionPoints -> Defined in the main process
variations -> outside of the main process (before, after, replace)

or simply list all variations (without defining the common actions)


open: how to specify which variation to take?

A =  1 & 2 & 3 & (  a | b  | c ) & 4  
& replaced by ,
how to select variation?
where to describe how to select
state?


activity:
call
call
variation{
case
case
}
call
call