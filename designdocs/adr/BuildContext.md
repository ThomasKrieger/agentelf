What classes to inclucde into the prompt?

For newly generated classes use @.. to include classes
we need to follow java doc syntax so probably @param ...
and be able to parse java doc tags

perhaps also provide {{} for special cases ?

if we load the classes for change, we also have the referenced arrays in the include
transitive. open how far
newly generated all classes wich are part of the model, but also @.. needed for external classes
which are to be included transitive

-> Stop at Seams definition, module defintiton, layers