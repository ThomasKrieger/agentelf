Same abstraction layer:
Example
f: Algebaric Data Type -> Abstract Data Type

from higher to lower Abstraction:
Not a function (e.g. additional information needed for example llm)
but of we have done the inverse we can keep the additional information
an this becomes a function

from lower to higher: function but no inverse exist


each model has its own mathematic foundation
but we need no abstraction of those foundations so long
as we can map one model to the other
same as no meta model but only n small models

perhaps category theory for the mapping between to models
