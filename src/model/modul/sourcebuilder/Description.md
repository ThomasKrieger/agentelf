Two cases:
   Method Builder
     Build a method for example for activity
     or design pattern for example wrapper
   Call Builder
      Build a call used inside a prompt
   class builder -> adt, builder for adt


based on java poet?


methodbuilder  
   add variable declararation
      add methodCall
      add parameter
      add  if statement
      add for loop



open return values and parameter declarations

for adt:
record
interface

implements 
field