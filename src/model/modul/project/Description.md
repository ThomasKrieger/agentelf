Describes where to find the sources, the jars, the model jira tickets and so on
should probably be independent of the project type e.g. maven, gradle or other
typically jars are accessible on a maven repo of download?

we can read gradle using:https://docs.gradle.org/current/userguide/tooling_api.html
and maven using Maven Model Builder and Maven Resolver